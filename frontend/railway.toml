# Railway config — Next.js frontend service
[build]
dockerfilePath = "/frontend/Dockerfile"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
